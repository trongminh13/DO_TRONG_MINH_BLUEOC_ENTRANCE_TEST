# Task 2: Sum of Top Two Integers

Sum the two largest integers in an array in a single O(n) pass.

## Run

```bash
node index.js
```

## Tests

9 positive cases (spec example, two elements, negatives, duplicates, sorted arrays, mixed signs, zeroes) plus 3 error cases (empty, single element, non-array).
