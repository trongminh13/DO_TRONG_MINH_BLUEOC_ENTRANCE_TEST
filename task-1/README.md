# Task 1: String Length Frequency

Find the strings whose lengths appear most frequently in an array.

## Run

```bash
node index.js
```

## Tests

8 test cases covering: empty array, all same length, single string, clear winners, ties, empty strings, and the spec example.
